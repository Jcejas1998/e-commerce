const Users = require('../models/useModel')

const userCtrl={
    register: async (req,res)=>{
        try {
            
        } catch (error) {
            
        }
    }
}

module.exports = userCtrl